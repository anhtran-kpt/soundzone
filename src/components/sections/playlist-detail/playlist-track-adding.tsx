"use client";

import SectionHeading from "@/components/ui/section-heading";

export const PlaylistTrackAdding = ({
  playlistSlug,
}: {
  playlistSlug: string;
}) => {
  return (
    <section>
      <SectionHeading heading="New songs" />
    </section>
  );
};
