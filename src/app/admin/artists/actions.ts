// import { createArtistSchema } from "@/schemas";

// export async function generateSlug(name: string) {

// }

// export async function createArtist(data: unknown) {
//   const parsed = createArtistSchema.safeParse(data);

//   if (!parsed.success) {
//     throw new Error("Validation failed");
//   }

//   const { name, bio, avatarUrl, coverUrl, nationality } = parsed.data;

//   await

//   return artist;
// }
